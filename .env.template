# NexuSphere Environment Template# NexuSphere environment template

# =================================# -------------------------------

# Copy this file to `.env` and fill in your values# Copy this file to `.env`, then fill in the blanks that mention LIVE mode.

# Development: leave defaults | Production: use Secret Manager# Leave values as-is for a practice run on your laptop. Replace them with

# production-safe secrets before going live.

###############################################################################

# Flask Core###############################################################################

################################################################################ Website address & behaviour

SECRET_KEY=change-me-to-random-32-plus-chars-in-production###############################################################################

FLASK_ENV=developmentSECRET_KEY=dev-change-this-key-before-live

FLASK_DEBUG=1FLASK_ENV=development

FLASK_DEBUG=1

###############################################################################PRIMARY_DOMAIN=nexusphere.localhost

# Multi-tenant ConfigurationADMIN_SUBDOMAIN=admin

###############################################################################TENANT_DEV_PORT=5000

PRIMARY_DOMAIN=nexusphere.localhostTENANT_PUBLIC_PORT=8080

ADMIN_SUBDOMAIN=adminTENANT_URL_SCHEME=http

TENANT_DEV_PORT=5000TENANT_ENFORCE_HOST=false

TENANT_PUBLIC_PORT=8080

TENANT_URL_SCHEME=http###############################################################################

# Database

##############################################################################################################################################################

# DatabaseDATABASE_URL=postgresql://email_user:email_pass@db:5432/email_draft_db

###############################################################################DB_PASSWORD=email_pass

DATABASE_URL=postgresql://email_user:email_pass@db:5432/email_draft_dbSUPERADMIN_PASSWORD=admin123

DB_PASSWORD=email_pass

SUPERADMIN_PASSWORD=admin123###############################################################################

# AI keys & models

##############################################################################################################################################################

# OpenAI APIOPENAI_API_KEY=put-your-openai-api-key-here

###############################################################################ZEROENTROPY_API_KEY=

OPENAI_API_KEY=your-openai-api-key-hereZEROENTROPY_MODEL=zerank-1

# Optional overrides (defaults defined in config.py):NOVITA_API_KEY=

#EMBEDDING_MODEL=text-embedding-3-largeNOVITA_RERANK_MODEL=qwen/qwen3-reranker-8b

#EMBEDDING_DIM=1024MULTIQUERY_MODEL_DEFAULT=gpt-5-mini

#OPENAI_RESPONSES_MODEL=gpt-4o-mini

#VISION_MODEL=gpt-4o-mini###############################################################################

#DEFAULT_RESPONSE_LANGUAGE=en# File uploads & processing limits

###############################################################################

###############################################################################MAX_UPLOAD_FILE_MB=200

# Reranking Providers (optional)MAX_PDF_PAGES=200

###############################################################################MAX_IMAGES_PER_FILE=200

ZEROENTROPY_API_KEY=FILE_PROCESSING_LOG_RETENTION_DAYS=30

ZEROENTROPY_MODEL=zerank-1

NOVITA_API_KEY=###############################################################################

NOVITA_RERANK_MODEL=qwen/qwen3-reranker-8b# Email sending (system notifications)

###############################################################################

###############################################################################MAIL_SERVER=smtp.example.com

# Retrieval & Search Configuration (optional overrides)MAIL_PORT=465

###############################################################################MAIL_USE_TLS=False

#VECTOR_TOP_K_DEFAULT=5MAIL_USE_SSL=True

#RERANK_TOP_K_DEFAULT=5MAIL_USERNAME=no-reply@example.com

#RERANK_THRESHOLD_DEFAULT=0.6MAIL_PASSWORD=change-me

RERANK_PROVIDER=zeroentropyMAIL_DEFAULT_SENDER=no-reply@example.com

#MULTI_QUERY_ENABLED=trueSUPPORT_EMAIL=support@example.com

#MULTIQUERY_MODEL_DEFAULT=gpt-4o-mini

#INCLUDE_NEIGHBOR_CHUNKS_DEFAULT=true###############################################################################

# Email templates (optional)

# Hybrid search parameters (optional overrides):###############################################################################

#HYBRID_RRF_K=60SUPPORT_UPGRADE_SUBJECT=[OmniResponse] Prośba o dodatkowe pakiety – {organization}

#PREFETCH_LIMIT=20SUPPORT_UPGRADE_BODY=Dzień dobry,\n\nProszę o zwiększenie limitu pakietów dla organizacji: {organization}.\nAdministrator: {admin_name} ({admin_email}).\nLiczba dodatkowych pakietów: ...\nPozdrawiam,

#COLBERT_CANDIDATES=15PROJECT_USAGE_WARN_SUBJECT=[NexuSphere] Zbliżające się wykorzystanie pakietów – {project}

#RRF_DENSE_WEIGHT=0.6PROJECT_USAGE_WARN_BODY=Dzień dobry,\n\nProjekt "{project}" w organizacji {organization} przekroczył 95% wykorzystania {which}.\nProsimy o przydzielenie dodatkowego pakietu, aby uniknąć przerw w działaniu.\n\nPozdrawiamy,\nZespół NexuSphere

#RRF_SPARSE_WEIGHT=0.3SUPPORT_SUSPENSION_SUBJECT=[NexuSphere] Prośba o przywrócenie dostępu – {organization}

#RRF_COLBERT_WEIGHT=0.1SUPPORT_SUSPENSION_BODY=Dzień dobry,\n\nProszę o weryfikację i przywrócenie dostępu dla organizacji: {organization}.\nUżytkownik: {user_email}.\n\nDziękuję.



##############################################################################################################################################################

# Qdrant Vector Database# Security & usage limits

##############################################################################################################################################################

QDRANT_HOST=qdrantMAX_TOKENS_PER_PROJECT=2000000

QDRANT_PORT=6333MAX_EMAIL_ACCOUNTS_PER_PROJECT=3

QDRANT_API_KEY=your-qdrant-api-keyMAX_OUTPUT_TOKENS=6000

#QDRANT_DEBUG=falseSESSION_COOKIE_SECURE=false

#QDRANT_ALLOW_INSECURE_FALLBACK=falseREMEMBER_COOKIE_SECURE=false

SECURITY_CSP_STRICT=false

###############################################################################CSP_UPGRADE_INSECURE_REQUESTS=false

# BGE-M3 Embeddings Service (optional overrides)ENABLE_HSTS=false

###############################################################################HSTS_MAX_AGE=15552000

#BGE_M3_BASE_URL=http://bge_m3:8000HSTS_INCLUDE_SUBDOMAINS=true

#BGE_M3_TIMEOUT=30.0HSTS_PRELOAD=false

#BGE_RETRY_ATTEMPTS=3METRICS_DISABLED=true

#BGE_RETRY_BACKOFF_SECS=1.0

#BGE_RETRY_JITTER_SECS=0.5###############################################################################

#COLBERT_DIM=1024# Antivirus scanner

###############################################################################

###############################################################################CLAMAV_STARTUP_GRACE_SECS=180

# File Upload & Processing (optional overrides)CLAMAV_CONNECT_RETRIES=5

###############################################################################CLAMAV_RETRY_DELAY_SECS=2.0

#MAX_CONTENT_LENGTH=536870912

#UPLOAD_FOLDER=/app/data/uploads###############################################################################

#MAX_UPLOAD_FILE_MB=200# Google Workspace OAuth (leave blank until configured)

#MAX_PDF_PAGES=200###############################################################################

#MAX_IMAGES_PER_FILE=200ORG_GMAIL_CLIENT_ID=

#FILE_PROCESSING_LOG_RETENTION_DAYS=30ORG_GMAIL_CLIENT_SECRET=

ORG_GMAIL_REDIRECT_URI=http://127.0.0.1:8080/oauth/gmail/callback

###############################################################################ORG_GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.compose

# Usage Limits (optional overrides)ORG_GMAIL_AUTH_URI=https://accounts.google.com/o/oauth2/v2/auth

###############################################################################ORG_GMAIL_TOKEN_URI=https://oauth2.googleapis.com/token

#MAX_TOKENS_PER_PROJECT=2000000

#MAX_OUTPUT_TOKENS=20000###############################################################################

# Microsoft 365 / Outlook OAuth (leave blank until configured)

##############################################################################################################################################################

# Security - Sessions & CookiesORG_OUTLOOK_CLIENT_ID=

###############################################################################ORG_OUTLOOK_CLIENT_SECRET=

SESSION_COOKIE_SECURE=falseORG_OUTLOOK_SCOPES=offline_access https://outlook.office.com/IMAP.AccessAsUser.All

REMEMBER_COOKIE_SECURE=falseORG_OUTLOOK_REDIRECT_URI=

#SESSION_COOKIE_SAMESITE=LaxORG_OUTLOOK_TENANT=

#REMEMBER_COOKIE_SAMESITE=LaxORG_OUTLOOK_AUTH_URI=

#PERMANENT_SESSION_LIFETIME=28800ORG_OUTLOOK_TOKEN_URI=



##############################################################################################################################################################

# Security - HTTP Headers# Encryption key

##############################################################################################################################################################

SECURITY_CSP_STRICT=falseENCRYPTION_KEY=replace-with-your-fernet-key

CSP_UPGRADE_INSECURE_REQUESTS=false###############################################################################

#SECURITY_CSP=# Flask / multi-tenant core

#REFERRER_POLICY=strict-origin-when-cross-origin###############################################################################

#PERMISSIONS_POLICY=geolocation=(), microphone=(), camera=()

#ENABLE_HSTS=false# SECRET_KEY — klucz sesji Flask; dev: losowy stały string, prod: 32+ znaków przechowywany w Secret Manager

#HSTS_MAX_AGE=15552000SECRET_KEY=change-me-in-production

#HSTS_INCLUDE_SUBDOMAINS=true# FLASK_ENV — tryb środowiska; dev: development, prod: production

#HSTS_PRELOAD=falseFLASK_ENV=production

# FLASK_DEBUG — włącznik debugera; dev: 1 gdy debugujesz, prod: 0

###############################################################################FLASK_DEBUG=0

# Rate Limiting & Monitoring# PRIMARY_DOMAIN — domena bazowa tenantów; dev: nexusphere.localhost lub *.sslip.io, prod: publiczny apex domeny

###############################################################################PRIMARY_DOMAIN=nexusphere.localhost

#RATELIMIT_STORAGE_URI=memory://# ADMIN_SUBDOMAIN — subdomena panelu admina; dev: admin, prod: nazwa wg polityki bezpieczeństwa (np. admin)

METRICS_DISABLED=trueADMIN_SUBDOMAIN=admin

# TENANT_DEV_PORT — port lokalnej aplikacji Flask; dev: 5000 (flask run), prod: puste (za reverse proxy)

###############################################################################TENANT_DEV_PORT=5000

# WebSocket Configuration (optional overrides)# TENANT_PUBLIC_PORT — port reverse proxy; dev: 8080 (nginx), prod: 443 lub puste gdy TLS terminowany

###############################################################################TENANT_PUBLIC_PORT=8080

#WS_MAX_CONNECTIONS_PER_USER=1# TENANT_URL_SCHEME — schemat URL; dev: http, prod: https

#WS_IDLE_TIMEOUT=90TENANT_URL_SCHEME=http

# TENANT_ENFORCE_HOST — wymuszanie hostów tenantów; dev: false przy tunelowaniu, prod: true

###############################################################################TENANT_ENFORCE_HOST=false

# ClamAV Antivirus (optional overrides)

##############################################################################################################################################################

#CLAMAV_STARTUP_GRACE_SECS=180# Baza danych

#CLAMAV_CONNECT_RETRIES=5###############################################################################

#CLAMAV_RETRY_DELAY_SECS=2.0

# DATABASE_URL — URL bazy danych; dev: postgres z docker-compose, prod: zarządzana baza (np. RDS)

###############################################################################DATABASE_URL=postgresql://email_user:email_pass@db:5432/email_draft_db

# Encryption Key (REQUIRED - generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")# DB_PASSWORD — hasło fallbackowe wykorzystywane przy konstrukcji URI; dev: email_pass, prod: tajny sekret serwisowy

###############################################################################DB_PASSWORD=email_pass

ENCRYPTION_KEY=your-32-byte-base64-fernet-key-here

# Superadmin password; dev -> admin123, prod -> set in secret manager
SUPERADMIN_PASSWORD=admin123

###############################################################################
# OpenAI / modele AI
###############################################################################

# OPENAI_API_KEY — klucz API OpenAI; dev: klucz konta testowego, prod: przechowywany poza repozytorium
OPENAI_API_KEY=sk-your-openai-api-key-here
# EMBEDDING_MODEL — nazwa modelu embeddingów; dev: text-embedding-3-small (tańszy), prod: text-embedding-3-large
#EMBEDDING_MODEL=text-embedding-3-large
# EMBEDDING_DIM — wymiar embeddingu zgodny z modelem; dev/prod: 1024 dla text-embedding-3-large
#EMBEDDING_DIM=1024
# OPENAI_RESPONSES_MODEL — model generacji odpowiedzi; dev: gpt-5-nano, prod: gpt-5-mini lub zgodny z certyfikacją
#OPENAI_RESPONSES_MODEL=gpt-5-mini
# VISION_MODEL — model OCR/vision; dev: gpt-5-nano, prod: gpt-5-mini lub dedykowany
#VISION_MODEL=gpt-5-mini

###############################################################################
# ZeroEntropy / Novita AI – reranking
###############################################################################

# ZEROENTROPY_API_KEY — klucz ZeroEntropy; dev: testowy, prod: przechowywać poza repozytorium
ZEROENTROPY_API_KEY=
# ZEROENTROPY_MODEL — domyślny model rerankera ZeroEntropy (zerank-1 lub zerank-1-small)
ZEROENTROPY_MODEL=zerank-1
# NOVITA_API_KEY — klucz Novita AI; dev: testowy, prod: Secret Manager
NOVITA_API_KEY=
# NOVITA_RERANK_MODEL — domyślny model rerankera Novita (np. qwen/qwen3-reranker-8b)
NOVITA_RERANK_MODEL=qwen/qwen3-reranker-8b

###############################################################################
# Retrieval & reranking parametry domyślne
###############################################################################

# VECTOR_TOP_K_DEFAULT — ile dokumentów pobierać z Qdrant (top K)
VECTOR_TOP_K_DEFAULT=5
# RERANK_TOP_K_DEFAULT — ile dokumentów zwrócić po rerankingu
RERANK_TOP_K_DEFAULT=5
# RERANK_THRESHOLD_DEFAULT — minimalny wynik podobieństwa akceptowany po rerankingu (0-1)
RERANK_THRESHOLD_DEFAULT=0.6
# RERANK_PROVIDER — domyślny provider rerankingu: none | zeroentropy | novita
RERANK_PROVIDER=zeroentropy
# MULTIQUERY_MODEL_DEFAULT — model generujący zapytania multiquery (np. gpt-5-mini, gpt-5-nano)
MULTIQUERY_MODEL_DEFAULT=gpt-5-nano
# DODAWANIE SĄSIADUJĄCYCH CHUNKÓW, jeżeli jest jeden z rerankingu, dodaj sąsiadujące
INCLUDE_NEIGHBOR_CHUNKS_DEFAULT=true

###############################################################################
# Qdrant (wektorowe wyszukiwanie)
###############################################################################

# QDRANT_HOST — host instancji Qdrant; dev: qdrant (docker), prod: nazwa wewnętrznego hosta lub endpointu SaaS
QDRANT_HOST=qdrant
# QDRANT_PORT — port API; dev: 6333, prod: 6333 lub 443 za proxy
QDRANT_PORT=6333
# QDRANT_API_KEY — klucz API Qdrant; dev: token testowy, prod: tajny token administracyjny
QDRANT_API_KEY=replace-with-qdrant-api-key
# QDRANT_DEBUG — rozszerzone logowanie; dev: true, prod: false
QDRANT_DEBUG=true
# QDRANT_ALLOW_INSECURE_FALLBACK — zezwól na HTTP fallback; dev: true jeśli brak TLS, prod: false
#QDRANT_ALLOW_INSECURE_FALLBACK=false

###############################################################################
# Konfiguracja uploadów i przetwarzania dokumentów
###############################################################################

# MAX_CONTENT_LENGTH — maksymalny rozmiar requestu (B); dev: 536870912 (512 MB), prod: dopasuj do polityki
#MAX_CONTENT_LENGTH=536870912
# MAX_UPLOAD_FILE_MB — limit pojedynczego pliku; dev: 200, prod: 50-200 zależnie od polityki bezpieczeństwa
#MAX_UPLOAD_FILE_MB=200
# MAX_PDF_PAGES — limit stron PDF; dev: 200, prod: wg SLA
#MAX_PDF_PAGES=200
# MAX_IMAGES_PER_FILE — limit obrazów; dev: 200, prod: niżej aby ograniczyć koszty
#MAX_IMAGES_PER_FILE=200
# FILE_PROCESSING_LOG_RETENTION_DAYS — retencja logów (dni); dev: 30, prod: 30-90 w zgodzie z RODO
#FILE_PROCESSING_LOG_RETENTION_DAYS=30

###############################################################################
# E-mail (powiadomienia systemowe)
###############################################################################

# MAIL_SERVER — host SMTP; dev: localhost (MailHog), prod: host dostawcy
MAIL_SERVER=smtp.example.com
# MAIL_PORT — port SMTP; dev: 1025 lub 587, prod: 465 lub 587 zależnie od dostawcy
MAIL_PORT=465
# MAIL_USE_TLS — STARTTLS; dev: False dla MailHog, prod: True gdy korzystasz z portu 587
MAIL_USE_TLS=False
# MAIL_USE_SSL — połączenie SSL; dev: False, prod: True przy porcie 465
MAIL_USE_SSL=True
# MAIL_USERNAME — login skrzynki; dev: konto testowe, prod: serwisowy adres e-mail
MAIL_USERNAME=no-reply@example.com
# MAIL_PASSWORD — hasło skrzynki; dev: testowe hasło, prod: przechowywane w Secret Manager
MAIL_PASSWORD=change-me
# MAIL_DEFAULT_SENDER — nadawca powiadomień; dev: MAIL_USERNAME, prod: dedykowana skrzynka supportu
MAIL_DEFAULT_SENDER=no-reply@example.com
# SUPPORT_EMAIL — kontakt supportu; dev: support@nexusphere.local, prod: rzeczywisty adres wsparcia
#SUPPORT_EMAIL=support@example.com

###############################################################################
# Szablony wiadomości e-mail (opcjonalne nadpisania)
###############################################################################

# SUPPORT_UPGRADE_SUBJECT — temat prośby o pakiety; dev: wartość domyślna, prod: dopasuj do brandingu
#SUPPORT_UPGRADE_SUBJECT=[NexuSphere] Prośba o dodatkowe pakiety – {organization}
# SUPPORT_UPGRADE_BODY — treść prośby o pakiety; dev: domyślna, prod: spersonalizowana
#SUPPORT_UPGRADE_BODY=Dzień dobry,\n\nProszę o zwiększenie limitu pakietów dla organizacji: {organization}.\nAdministrator: {admin_name} ({admin_email}).\nLiczba dodatkowych pakietów: ...\nPozdrawiam,
# PROJECT_USAGE_WARN_SUBJECT — temat ostrzeżenia o limitach; dev: domyślny, prod: dopasuj styl brandu
#PROJECT_USAGE_WARN_SUBJECT=[NexuSphere] Zbliżające się wykorzystanie pakietów – {project}
# PROJECT_USAGE_WARN_BODY — treść ostrzeżenia; dev: domyślna, prod: dopasuj
#PROJECT_USAGE_WARN_BODY=Dzień dobry,\n\nProjekt "{project}" w organizacji {organization} przekroczył 95% wykorzystania {which}.\nProsimy o przydzielenie dodatkowego pakietu, aby uniknąć przerw w działaniu.\n\nPozdrawiamy,\nZespół NexuSphere
# SUPPORT_SUSPENSION_SUBJECT — temat reaktywacji konta; dev: domyślna, prod: formalna komunikacja
#SUPPORT_SUSPENSION_SUBJECT=[NexuSphere] Prośba o przywrócenie dostępu – {organization}
# SUPPORT_SUSPENSION_BODY — treść reaktywacji; dev: domyślna, prod: zgodna z polityką
#SUPPORT_SUSPENSION_BODY=Dzień dobry,\n\nProszę o weryfikację i przywrócenie dostępu dla organizacji: {organization}.\nUżytkownik: {user_email}.\n\nDziękuję.

###############################################################################
# Integracje Google Workspace
###############################################################################

# ORG_GMAIL_CLIENT_ID — klient OAuth Gmail; dev: credentials testowego projektu Google, prod: dedykowany projekt dla klienta
ORG_GMAIL_CLIENT_ID=replace-with-google-client-id
# ORG_GMAIL_CLIENT_SECRET — tajny klucz OAuth; dev: secret testowy, prod: w Secret Manager
ORG_GMAIL_CLIENT_SECRET=replace-with-google-client-secret
# ORG_GMAIL_REDIRECT_URI — redirect URL; dev: http://127.0.0.1:8080/oauth/gmail/callback, prod: https://twoja-domena/oauth/gmail/callback
ORG_GMAIL_REDIRECT_URI=http://127.0.0.1:8080/oauth/gmail/callback
# ORG_GMAIL_SCOPES — zakresy OAuth; dev: https://www.googleapis.com/auth/gmail.compose, prod: minimalny wymagany zakres
ORG_GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.compose
# ORG_GMAIL_AUTH_URI — endpoint autoryzacji; dev/prod: https://accounts.google.com/o/oauth2/v2/auth
ORG_GMAIL_AUTH_URI=https://accounts.google.com/o/oauth2/v2/auth
# ORG_GMAIL_TOKEN_URI — endpoint tokenów; dev/prod: https://oauth2.googleapis.com/token
ORG_GMAIL_TOKEN_URI=https://oauth2.googleapis.com/token

###############################################################################
# Integracje Outlook / Microsoft 365
###############################################################################

# ORG_OUTLOOK_CLIENT_ID — klient OAuth Azure; dev: puste jeśli nie testujesz, prod: GUID aplikacji Azure AD
ORG_OUTLOOK_CLIENT_ID=
# ORG_OUTLOOK_CLIENT_SECRET — tajny klucz OAuth; dev: puste, prod: sekrety w Azure Key Vault
ORG_OUTLOOK_CLIENT_SECRET=
# ORG_OUTLOOK_SCOPES — zakresy OAuth; dev/prod: offline_access https://outlook.office.com/IMAP.AccessAsUser.All (dostosuj dla Graph)
ORG_OUTLOOK_SCOPES=offline_access https://outlook.office.com/IMAP.AccessAsUser.All
# ORG_OUTLOOK_REDIRECT_URI — redirect URL; dev: http://127.0.0.1:8080/oauth/outlook/callback, prod: https://twoja-domena/oauth/outlook/callback
ORG_OUTLOOK_REDIRECT_URI=
# ORG_OUTLOOK_TENANT — tenant ID; dev: common, prod: ID tenant'a klienta
ORG_OUTLOOK_TENANT=
# ORG_OUTLOOK_AUTH_URI — endpoint autoryzacji; dev/prod: pozostaw puste aby użyć wartości domyślnej Microsoft
ORG_OUTLOOK_AUTH_URI=
# ORG_OUTLOOK_TOKEN_URI — endpoint tokenów; dev/prod: pozostaw puste aby użyć wartości domyślnej Microsoft
ORG_OUTLOOK_TOKEN_URI=

###############################################################################
# Bezpieczeństwo HTTP
###############################################################################

# SECURITY_CSP_STRICT — strict CSP z nonce; dev: false dla łatwiejszego debugowania, prod: true po obsłudze nonce w froncie
SECURITY_CSP_STRICT=false
# CSP_UPGRADE_INSECURE_REQUESTS — automatyczny upgrade http->https; dev: false (brak TLS), prod: true
CSP_UPGRADE_INSECURE_REQUESTS=false
# SECURITY_CSP — niestandardowa polityka CSP; dev: puste by użyć domyślnej logiki, prod: ustaw konkretną politykę
SECURITY_CSP=
# REFERRER_POLICY — polityka referrer; dev: strict-origin-when-cross-origin, prod: np. no-referrer lub strict-origin
#REFERRER_POLICY=strict-origin-when-cross-origin
# PERMISSIONS_POLICY — kontrola API przeglądarki; dev/prod: geolocation=(), microphone=(), camera=()
#PERMISSIONS_POLICY=geolocation=(), microphone=(), camera=()
# ENABLE_HSTS — nagłówek HSTS; dev: false (brak TLS), prod: true po wdrożeniu HTTPS
#ENABLE_HSTS=false
# HSTS_MAX_AGE — czas HSTS (sekundy); dev: 15552000, prod: 31536000
#HSTS_MAX_AGE=15552000
# HSTS_INCLUDE_SUBDOMAINS — obejmuje subdomeny; dev: true, prod: true jeśli wszystkie subdomeny działają na HTTPS
#HSTS_INCLUDE_SUBDOMAINS=true
# HSTS_PRELOAD — zgłoszenie do listy preload; dev: false, prod: true dopiero po spełnieniu wymagań
#HSTS_PRELOAD=false

###############################################################################
# Ratelimiting i monitoring
###############################################################################

# RATELIMIT_STORAGE_URI — backend limitera; dev: memory://, prod: redis://redis:6379/0
#RATELIMIT_STORAGE_URI=memory://
# METRICS_DISABLED — wyłączenie endpointu /metrics; dev: true jeśli nie monitorujesz, prod: false dla Prometheusa
METRICS_DISABLED=true

###############################################################################
# Skany antywirusowe (ClamAV)
###############################################################################

# CLAMAV_STARTUP_GRACE_SECS — czas oczekiwania na start ClamAV; dev: 180 (wolny kontener), prod: 60-120
CLAMAV_STARTUP_GRACE_SECS=180
# CLAMAV_CONNECT_RETRIES — liczba ponowień połączenia; dev: 5, prod: 5-10
CLAMAV_CONNECT_RETRIES=5
# CLAMAV_RETRY_DELAY_SECS — odstęp między próbami; dev: 2.0, prod: 1.0-2.0
CLAMAV_RETRY_DELAY_SECS=2.0

###############################################################################
# Klucze kryptograficzne
###############################################################################

# ENCRYPTION_KEY — 32-bajtowy klucz Fernet (base64); dev: wygeneruj lokalnie i zapisz w .env, prod: Secret Manager/Key Vault
ENCRYPTION_KEY=your-32-byte-base64-encoded-key-here
